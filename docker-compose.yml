version: '3.3'
services:
  web:
    build:
      context: .
      dockerfile: docker/dev.web.dockerfile
    command: npm run dev
    ports:
      - 8080:8080
    volumes:
      - ./the-tavern:/usr/src/app
